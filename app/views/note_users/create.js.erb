$("<%= escape_javascript("#related_user-" + @related_user.id.to_s) %>")
  .html("<%= escape_javascript(render partial: 'notes/release_related_user',
    locals: { note: @note, related_user: @related_user }) %>");
