<%= form_for(related_user.related_note_users.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :note_id, note.id %></div>
  <div><%= hidden_field_tag :related_user_id, related_user.id %></div>
  <%= image_submit_tag(related_user.image_url.url, class: 'not_related_user_image', alt: related_user.login_user_id) %>
<% end %>

