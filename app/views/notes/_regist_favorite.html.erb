<%= form_for(current_user.favorite_note_relations.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :note_id, note.id %></div>
  <%= image_submit_tag("gray_heart.png", class: 'favorite_off', alt: 'Regist') %>
<% end %>

